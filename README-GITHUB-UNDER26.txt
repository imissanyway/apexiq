ApexIQ v4.9.9.11 GitHub-safe under-26MB deploy package

Upload the CONTENTS of this folder to GitHub Pages.

This package keeps the same badge filenames and paths:
assets/approved-badges/*.png

To get under 26 MB, the badge PNGs are web-optimized to 416x416.
The source/master 512x512 transparent badges are still preserved in the previous full badge ZIP.
The website code/path structure is unchanged, so the badge system should not break.

Included:
- index.html
- worker JS
- badge manifests/reports
- all 120 transparent badge PNGs
