ApexIQ v4.9.9.21 Meta + Theme + Badge Fix

Built from v4.9.9.19 stable, not from the v4.9.9.20 theme experiment.

Fixed:
- Gibraltar false 20B source fixed on the frontend display:
  - Blocks bare “20B”, “20”, “20 bomb”, “20 kill”, bare “4K”, and 4000-style generic labels from regular ALS badge slots unless the visible text proves Wake/Wrath.
  - Blocks badge images whose src points to 20B/4K collection art unless the visible ALS text proves Wake/Wrath.
  - Regular ALS badges such as Apex Gibraltar I and Shot Caller still show.
- Profile mini badge case shows unlocked ApexIQ badges only.
- Legend renderer is protected from the previous __score/string crash.
- Legend snapshot waits for useful rows instead of showing fake roster-order 0-stat cards.
- Meta/Pick-rate panel now loads /api/meta after boot and when Meta is opened.
- If /api/meta fails, Meta shows a roster fallback instead of an empty broken panel.

Theme Studio:
- Removed the messy v4.9.9.20 generated pink wall.
- Replaced it with a compact mobile-safe theme grid.
- 224 unique themes: 28 legend-based theme families × 8 moods.
- Default tab is Featured instead of dumping all 224 at once.
- Search, category tabs, dark/light/toggle, and mode behavior all remain.
- Cards are compact and forced away from global pink button styling.
- Theme colors apply through CSS variables and work with dark/light modes.

Validation:
- HTML JavaScript syntax check passed.
- Worker syntax check passed.
