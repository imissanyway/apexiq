ApexIQ v4.9.9.30 Clean Meta + Percent Restore

Built clean from v4.9.9.24 frozen stable, not from the broken v25-v29 chain.

What this fixes:
- Removes the stacked-script conflict that let old meta renderers blank the Meta panel.
- Meta panel is force-rendered from one v4.9.9.30 renderer and cannot stay empty.
- Meta shows pick rate, trend, average rank, and average level.
- Worker fallbackPickratesMeta now returns visible ALS snapshot rows instead of a 0-only/blank roster.
- Stat cards stay readable with dark values on light cards.
- Every numeric stat now gets a percentage row.
  - Exact ALS screenshot matches are marked with real Top % / rank.
  - Other numeric stats get an AIQ estimate so the UI no longer has missing percentage rows.
- The percentage rows are added after the v4.9.9.24 badge renderer runs, so badges are not re-parsed or broken.
- v4.9.9.24 badge behavior is preserved:
  - owner 20B/4K restored
  - confirmed false Gibraltar 20B blocked
  - normal ALS badges remain visible

Validation:
- HTML JavaScript syntax check passed.
- Worker syntax check passed.
