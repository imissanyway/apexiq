ApexIQ v4.9.9.25 Frozen Base + Meta/Percent Add-on

Base:
- Built from v4.9.9.24 Collection Badge Restore.
- v4.9.9.24 was frozen separately as rollback before this patch.

Do-not-break rule:
- Badge Locker, mini display case, owner collection restoration, and confirmed false Gibraltar 20B block are not redesigned.
- v4.9.9.25 is additive: Meta board display + stat percentile/rank display only.

Added:
- Meta board now displays pick rate, trend, average rank, and average level when /api/meta returns those fields.
- Meta cards use legend images as faint background cards similar to the ALS reference.
- Legend cards can display Top % and #rank bars on individual stats when the Worker returns percentile/rank fields.
- No fake percentages are invented. If ALS/Worker does not return a percentile/rank, the card keeps the normal stat value only.

Worker:
- New Worker: apexiq-api-worker-v4.9.9.25-meta-stat-percent-additive.js
- Preserves tracker fields: percentile, topPercent, rank, total.
- /api/meta parsing is more robust for ALS text split across separate nodes:
  - Avg rank
  - Platinum/Gold/etc.
  - level ###

Rollback:
- Use ApexIQ-v4.9.9.24-FROZEN-STABLE-DO-NOT-TOUCH.zip if anything in v4.9.9.25 behaves wrong.
